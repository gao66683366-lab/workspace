# 2026-03-09 正式上下文检查点

## 时间范围
- 覆盖：2026-03-09 上午至 15:42（GMT+8）

## 一、当前总目标
在保证 OpenClaw / Feishu 链路稳定、显示完整、执行不中断的前提下，持续推进：
1. 飞书手机端 / 电脑端显示不同步问题排查；
2. 上下文连续性治理，避免长会话后失忆或口径漂移；
3. 铁路线路智能检测机器人项目资料、参数、文档口径对齐。

## 二、用户硬约束（不可丢）
- 用中文沟通。
- 不能失忆，不能前后口径打架，不能说胡话。
- 先把资料读透、口径对齐，再继续推演。
- 不要只讲结论，要边处理边展示过程。
- 输出优先按“问题页 / 回答页”。
- 功能/显示完整、执行链路完整不中断、飞书连接持续稳定。
- 对上下文治理方案，由助手主动决策，不反复征求用户。

## 三、飞书双端不同步 / 控制链路主线
### 1. 已确认事实
- 当前更近的故障主线，不是项目参数，而是“多控制入口 / 双端显示不同步”。
- 本机排查结果：
  - `openclaw status`：Gateway 正常；
  - `openclaw gateway status`：仅见一个本地 gateway；
  - `node` 进程与 18789 监听结果：仅见一个本地 node 监听。
- 结论：本机未发现两个 gateway 实例同时运行。

### 2. 已执行动作
- 检查 `openclaw devices list --json` 与 `paired.json`；
- 发现两条 `openclaw-control-ui / webchat / operator` 记录；
- 识别主用 control-ui 与旧残留 control-ui；
- 备份 `paired.json`；
- 执行旧残留 operator token 的 `revoke`；
- 执行旧残留设备记录的 `remove`；
- 复核 paired devices，仅保留主用控制入口。

### 3. 已确认结果
- 收敛后链路未中断：Gateway 正常、Feishu 正常、主会话仍活跃。
- 控制层已从“多入口干扰”向“单主入口”收敛。

### 4. 当前未解点
- 仍拿不到飞书手机端 / 电脑端 UI 的真实显示时间戳；
- 因此暂不能可靠计算双端显示时间差。

### 5. 已识别入口补强方向
- 电脑端：Chrome extension relay。
- 手机端：iOS / Android node 接入。
- 当前状态：
  - Chrome profile 可用，但暂无 attach tab；
  - nodes status = 0，手机端 node 尚未建立。

## 四、技能仓库 / 上下文治理主线
### 1. 技能仓库登录
- 已完成 ClawHub 登录。
- 当前登录账号：`gao66683366-lab`。
- 本次最终成功方式：用户提供 token，助手本地执行 `clawhub login --token ... --no-browser` 落登录态。

### 2. 查找过程摘要
围绕“上下文管理 / 记忆管理 / 会话摘要”检索并筛选过的候选包括：
- `context-recovery`
- `context-budgeting`
- `memory-reme`
- `openclaw-summary`
- `memory-tiering`
- `memory-manager`
- `memory-hygiene`
等。

### 3. 最终采用方案
#### 主修复
- `context-recovery@1.2.2`
- 用途：在会话压缩、继续前文、上下文断裂时，优先恢复当前工作上下文。

#### 预防层
- `context-budgeting@1.0.0`
- 用途：在长会话中做信息分层、压缩前检查点、降低失忆概率。

#### 辅助摘要层
- `openclaw-summary@1.0.0`
- 用途：做对话摘要与阶段性小结，但不单独承担主记忆职责。

### 4. 已完成落地
- 已安装上述 3 个 skill；
- 已建立热记忆文件：`memory/hot/HOT_MEMORY.md`；
- 已在 `memory/2026-03-09.md` 追加本次治理记录；
- 已提交 git：`d53706c` / `Add context management skills and hot memory`。

## 五、当前采用的上下文管理规则
### 1. 四层结构
1. 目标层：当前任务、最新要求、硬约束；
2. 已确认事实层：参数、工况、排查结论、已确认状态；
3. 决策日志层：做了什么、为什么、结果如何；
4. 短期原始对话层：只保最近几轮原话。

### 2. 恢复顺序
后续若用户说“继续”“上午聊到哪了”“你是不是忘了”：
1. 先读 `memory/hot/HOT_MEMORY.md`；
2. 再读当日/前一日 memory；
3. 再按 `context-recovery` 思路恢复会话主线；
4. 回答时继续按“问题页 / 回答页”组织。

## 六、铁路线路智能检测机器人项目不可丢口径
- 主项目：铁路线路智能检测机器人。
- 推演前必须严格对齐：假定工况、关键参数、原始技术文档、已生成文档结论。
- 已确认重要参数：
  - 速度：3 km/h = 0.833 m/s = 833.3 mm/s；
  - 每天运行 2 小时，约 6 km；
  - 道钉/螺栓节距：700 mm；
  - 所有 2D 相机、3D 线激光均垂直朝下；
  - 两个 3D 线激光检测左右钢轨轨头轮廓；
  - 姿态传感器检测：水平、高低、轨向；
  - 6 个 2D 相机装在铁路小车车架上；
  - 拍轨面相机离钢轨约 180 mm；
  - 拍道钉螺栓相机离轨面约 270 mm；
  - 钢轨轨面实际宽度约 70.8 mm，工程统一按 72 mm 假定。

## 七、当前下一步
1. 继续寻找双端显示时间采集入口；
2. 保持单主入口与 Feishu 链路稳定；
3. 后续长会话中，持续把关键决策写入热记忆与今日日志；
4. 在恢复项目技术推演前，先核对原始资料与已确认口径。
