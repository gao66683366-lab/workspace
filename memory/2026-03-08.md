# 2026-03-08

- 用户确认助手身份名为“**小测**”，并明确希望所有可见显示口径尽量一致。
- 已统一工作区身份文件：`IDENTITY.md` 中姓名为“小测”，`USER.md` 补入用户偏好与当前主项目背景。
- 已查明 OpenClaw Dashboard 最初未正确显示部分信息的关键原因：**Control UI 缺少 gateway token，导致 Dashboard 处于 unauthorized / offline 视图**。
- 已在本地 Dashboard 中手动填入 gateway token 并连通，随后确认：
  - Dashboard 恢复正常连接；
  - 会话页 `Label` 已显示为“**小测**”；
  - 但 `Key` 仍保留系统内部会话键（如 `agent:main:...`），属于内部路由/索引标识。
- 已确认用户截图中显示的 `ou_fdf66...` **不是助手名称**，而是飞书对端用户/会话目标 ID；该字段属于消息路由标识，**不宜为了显示美观而强改**，应以安全为第一。
- 与用户重新对齐昨日项目主线：当前真正延续的工作，不是泛泛做系统架构，而是继续围绕 **欧普特 CC1 系列 GigE 工业面阵相机说明书** 做深入阅读与参数梳理。
- 用户要求：在继续任何推演前，先把以下四层重新梳理对齐：
  1. 假定工作场景；
  2. 关键参数口径；
  3. 原始技术文档中的已确认内容；
  4. 昨天已生成文档中的结论与修正点。
- 已重新确认并口头提交过的项目假定工况/边界条件包括：
  - 铁路线路智能检测机器人；
  - 4 个轮子在铁轨上运行；
  - 速度 `3 km/h = 0.833 m/s = 833.3 mm/s`；
  - 每天运行 `2 小时`；
  - 每天运行约 `6 km`；
  - 道钉/螺栓节距 `0.7 m = 700 mm`；
  - 所有 2D 工业相机、3D 线激光均为**垂直朝下**；
  - 两个 3D 线激光检测对象是**左右两个钢轨的轨头轮廓**；
  - 姿态参数传感器用于检测**水平、高低、轨向**；
  - 6 个 2D 相机安装在铁路小车车架上；
  - 拍轨面的 2D 相机离钢轨约 `180 mm`；
  - 拍道钉螺栓的 2D 相机离轨面约 `270 mm`；
  - 钢轨轨面实际宽度约 `70.8 mm`，后续为了视频裁剪和工程计算，统一按 `72 mm` 假定。
- 用户特别强调：**不能失忆、不能前后口径打架、不能说胡话**；所有文档输出、内容输出、技术方案制定都必须严格以已确认口径为准。
- 当前仍未完成的重点任务：
  - 把欧普特工业面阵相机使用说明书继续“彻底读完、读透”；
  - 将其中与项目直接相关的参数（分辨率、传感器、触发方式、I/O、电源、网络、采集控制等）系统提取；
  - 梳理原始文档与已生成文档之间的口径一致性；
  - 之后再继续推进覆盖长度、每米拍多少张、触发频率、数据量等推演。
- 用户要求：**等读完欧普特说明书后，第一时间告诉他。**
- 用户明确纠正协作机制：对需要持续跟踪、可见性强的任务，不能采用“先处理再汇报”的方式，必须改为“边处理边汇报”，并希望后续所有对话尽量按这种透明协作要求执行。
- 用户新增硬性协作规定：后续围绕铁路检测机器人长期协作时，必须把三块作为持续监听对象并优先保障：1) 功能/显示完整；2) 执行链路完整与不中断；3) 飞书连接持续稳定。要求发现哪里有问题就立刻解决哪里的问题，避免因断链、显示不全、执行不到位导致效率变慢。
