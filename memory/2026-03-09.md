# 2026-03-09

- 用户澄清：当前更近的故障主线不是项目参数，而是“**双网关控制飞书造成手机端和电脑端显示不同步**”。
- 用户要求继续推进，并同时保证：
  - 网络联通；
  - 通讯通畅、不能中断；
  - 双屏/双端尽量同步显示。
- 本机现场核查结果：
  - `openclaw status` 显示 **Gateway=local ws://127.0.0.1:18789**，token 鉴权正常；
  - `openclaw gateway status` 显示当前只有 **一个本地 gateway 服务**，配置与服务均指向同一份 `~/.openclaw/openclaw.json`；
  - `Get-Process node` + `netstat -ano | findstr 18789` 显示本机当前只有 **一个 node 进程**监听 `127.0.0.1:18789`；
  - 因此，**本机没有发现两个 gateway 实例同时运行**。
- 当前更可能的根因方向：
  1. 另一个设备/环境上还有第二套 OpenClaw/Feishu 接入在工作；
  2. 或者是一个 gateway 下挂了多个 control UI/operator 客户端，造成界面状态观感不一致；
  3. 或者飞书客户端（手机/桌面）自身会话状态刷新节奏不同，但这只是表现层，不是根因。
- 已确认：本机 `devices/paired.json` 中存在多个 operator/control-ui 配对记录，但这不等于多个 gateway 进程；更像是多个控制端曾接入过同一 gateway。
- 下一步要做的是：继续区分“远端第二 gateway” vs “同一 gateway 多控制端”这两类情况，再决定是否需要收敛为单一控制链路。

- 用户反复强调：不要空讲、不要停在“继续观察”，要**边处理边展示过程**，并尽快拿到“同一条消息在手机端和电脑端的显示时间”，计算双端时间差。
- 已查明并执行的控制层收敛动作：
  - 通过 `openclaw devices list --json` / `paired.json` 确认两条 `openclaw-control-ui / webchat / operator` 记录；
  - 判定 `6b6303...` 为当前主用 control-ui，`cd0e05...` 为旧残留；
  - 先备份 `C:\Users\DELL\.openclaw\devices\paired.json` 为 `paired.json.bak-20260309-0630`；
  - 执行 `openclaw devices revoke --device cd0e05... --role operator --json`，旧残留 token 成功撤销；
  - 随后执行 `openclaw devices remove cd0e05... --json`，旧残留记录已从 paired devices 表彻底移除；
  - 复核后当前 paired devices 只剩 3 条，其中仅 1 条主用 `openclaw-control-ui`。
- 已验证：在上述 `revoke` + `remove` 后，`openclaw status` 仍显示：
  - `Gateway` 正常；
  - `Feishu` channel = OK；
  - 主会话仍活跃；
  - 因此控制层收敛**没有打断链路**。
- 对“双端显示时间差”已形成明确判断：
  - 目前能直接拿到的是 OpenClaw / 会话侧时间（如用户消息时间戳、session_status 当前时间）；
  - 目前**拿不到**飞书手机端与电脑端 UI 的真实显示时间戳，因此还不能得出可靠秒级差值；
  - 问题核心已从“控制层多入口干扰”转为“如何打通手机端/电脑端显示时间采集入口”。
- 已查到的入口补强路径：
  1. **电脑端**：Chrome extension relay（Chrome 扩展中继）；
  2. **手机端**：iOS/Android node（节点）接入。
- 当前入口状态：
  - `nodes status` = 0，且 `openclaw nodes pending` 显示无待审批请求，因此手机端 node 入口尚未建立；
  - `browser profiles` 显示 `chrome` 配置可用，但 `tabs` = 0，说明没有已接入的 Chrome 标签页；
  - 已执行 `openclaw browser extension install`，本机已准备好扩展目录：`~\.openclaw\browser\chrome-extension`；
  - 说明电脑端入口补强已推进到“扩展文件已落地”，但浏览器标签页附着仍未完成。
- 用户后来要求输出结构固定为：
  - **问题页**：展示用户提交的问题原文 + 时间；
  - **回答页**：展示处理过程、回答内容、处理结果、完成时间；
  - 并要求“同步进行，不能等”，不要再来回绕。
- 后续需要延续的主线：
  1. 保持当前已收敛出的“单主入口 + 链路稳定”状态；
  2. 继续寻找**无需用户动手**即可打通的电脑端 / 手机端显示时间采集入口；
  3. 一旦任一端显示时间可采集，马上开始做“问题时间 / 回答时间 / 双端显示时间差”的数据表。
- 已完成技能仓库登录，当前登录账号为 `gao66683366-lab`。
- 已因用户明确指出“现在上下文出问题了”，安装上下文相关 skill：`context-recovery@1.2.2`、`context-budgeting@1.0.0`、`openclaw-summary@1.0.0`。
- 已决定后续上下文管理采用“三层组合”：`context-recovery` 负责恢复，`context-budgeting` 负责预防与检查点，`openclaw-summary` 负责辅助摘要。
- 已新建 `memory/hot/HOT_MEMORY.md`，整理当前主任务、用户硬约束、今日关键结论、项目高优先级口径、下一步动作，用于长会话压缩后的快速恢复。
- 下午后续排查中，已确认手机端飞书与电脑端飞书**都已登录**，因此双端不同步**不是登录态问题**。
- 已从日志中确认：Feishu 入站到 agent 调度非常快（例如 16:10:27.382 收到、16:10:27.401 dispatch，约 19ms），因此**不是飞书消息送达或 gateway 入站慢**。
- 已从本地日志抓到更接近根因的两条证据：
  1. `webchat connected` / `webchat disconnected code=1001` 反复出现，说明电脑端 `openclaw-control-ui` / WebChat 链路存在**频繁断连重连**；
  2. 多次出现 `lane wait exceeded`，等待量级到过 **9 秒** 和 **26 秒**，说明会话处理链路存在明显排队/等待。
- 因此，当前对“手机端与电脑端显示不同步”的判断已收敛为：**主链路和飞书入站正常，问题更像发生在电脑端 WebChat/Control UI 回显链路不稳定 + agent 处理等待偏长**。
- 用户在 16:23 左右明确纠正：真正最优先的问题不是继续泛查双端显示，而是**手机飞书端提交现在需要马上和 Open Cloud / 远程 Gateway 建立连接**；上午之前这条链路在 10~20 秒内可以完成，现在变差了。
- 已重新聚焦到 Open Cloud / remote 主线：
  - 当前 `~/.openclaw/openclaw.json` 明确仍为 `gateway.mode = local`；
  - `status --all` / 诊断结果显示当前目标仍是 `ws://127.0.0.1:18789`（local loopback），并未切到 remote/cloud；
  - 本地文档 `gateway/remote.md` 已确认远程模式需要 `gateway.remote.url` 与 `gateway.remote.token`（或 password）。
- 已按用户提示转向从**昨天与今天的 session jsonl** 中反查 remote/cloud 线索；目前已在今天会话记录中确认两类地址：
  - `ws://127.0.0.1:18789`
  - `http://192.168.0.30:18789/`
  但截至当前仍**未找到明确的 Open Cloud remote URL / token**。
- 后续续接时，优先主线应改为：
  1. 继续从 `C:\Users\DELL\.openclaw\agents\main\sessions\0207caf0-6f5d-4d61-a35a-e6141e78cc8c.jsonl`、`840c677f-499f-411e-baa9-729f31df774c.jsonl`、`ab89d68f-e192-44c7-86f8-a63037998cff.jsonl` 中查找 `wss://` / `remote` / `cloud` / token 线索；
  2. 如果拿到 `gateway.remote.url` 与 `gateway.remote.token`，立即把配置从 `local` 切到 `remote` 并验证；
  3. 不要再把主精力放在网页登录/门户页，而要优先解决“手机飞书端提交 → Open Cloud 连接”这条链路。
- 夜间继续排查后，用户把优先级再次收敛为：**先解决 Feishu / Gateway / OpenClaw(Open Cloud) 主链路**，电脑端 Control UI 画面问题降级处理。
- 已确认当前实际模型上游并非直连 OpenAI，而是 `gmn` 提供方：`baseUrl = https://gmn.chuangzuoli.com/v1`，`api = openai-responses`，主模型为 `gmn/gpt-5.4`。
- 已从日志确认主链路分段状态：
  - **Feishu → Gateway**：稳定可达，持续出现 `received message`、`dispatching to agent`、`dispatch complete`；
  - **Gateway 本地服务**：在线可达，`ws://127.0.0.1:18789` 正常；
  - **上游模型链路**：出现明确异常，日志多次报 `LLM request timed out`。
- 已确认当前真正拖慢体感的硬故障不是飞书断链，而是**上游模型请求超时**，并伴随会话处理等待：
  - `lane wait exceeded` 曾到 **37 秒**、**88 秒**；
  - `embedded run agent end ... isError=true error=LLM request timed out` 已至少出现两次。
- 与电脑端 Control UI 相关的链路问题已进一步定位：
  - `http://192.168.0.30:18789` 会触发 `control ui requires device identity (use HTTPS or localhost secure context)`；
  - `http://127.0.0.1:18789` / `http://localhost:18789` 才是对的本机入口；
  - 但 Control UI 仍存在 `gateway token missing` / `token mismatch`，导致页面不能稳定恢复；
  - 已查到当前 gateway 正确 token：`740a05b1eb5e2128f3ddcb0d596161b8f2a3c894b4de3027`。
- 为抢救电脑端页面，曾在 `openclaw.json` 中临时打开：
  - `gateway.controlUi.allowInsecureAuth=true`
  - `gateway.controlUi.dangerouslyDisableDeviceAuth=true`
  这属于**短时 break-glass 调试配置**，`openclaw status --deep` 已将其标为安全高风险；后续在主链路稳定后应尽快收回。
- 一次 `openclaw gateway restart` 曾出现 **timed out after 60s waiting for health checks**，但随后 Feishu WebSocket 仍重新拉起并可继续收发消息，说明“重启等待超时”不等于主链路彻底挂死。
- 当前最该延续的夜间主线已经改为：
  1. 继续盯 `gmn.chuangzuoli.com` 这条上游模型链路，确认是否网络抖动、上游服务慢、还是请求超时阈值问题；
  2. 把“飞书稳定性问题”和“电脑端 UI 不动”与“上游 LLM 超时”明确拆开，不要混为一谈；
  3. 等主链路稳定后，再回头收尾 Control UI 的 token / 安全上下文问题；
  4. 如果继续压缩上下文，优先记住：**Feishu/Gateway 本地基本是通的，真正影响响应稳定性的核心证据目前是上游 LLM timeout。**
